/**
 * Orchestration engine abstractions and AgentScope-Java backed
 * implementation. The engine receives a {@code SessionContext} and
 * returns a result string; sub-agent dispatch is an internal concern.
 *
 * <p>Package-level {@link org.jspecify.annotations.NullMarked} — all
 * unannotated types in this package are non-null by default.</p>
 */
@NullMarked
package com.orch.hub.orchestration;

import org.jspecify.annotations.NullMarked;
