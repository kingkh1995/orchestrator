/**
 * Shared {@code @ConfigurationProperties} types. Currently holds
 * {@code OrchLLMProperties}; cross-feature config classes (messaging,
 * A2A) live next to the feature they configure.
 *
 * <p>Package-level {@link org.jspecify.annotations.NullMarked} — all
 * unannotated types in this package are non-null by default.</p>
 */
@NullMarked
package com.orch.hub.config;

import org.jspecify.annotations.NullMarked;
