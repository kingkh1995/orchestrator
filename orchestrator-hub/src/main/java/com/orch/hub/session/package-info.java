/**
 * Per-session state passed through the orchestration engine. Tracks the
 * session id, in-flight task list, and arbitrary state map.
 *
 * <p>Package-level {@link org.jspecify.annotations.NullMarked} — all
 * unannotated types in this package are non-null by default.</p>
 */
@NullMarked
package com.orch.hub.session;

import org.jspecify.annotations.NullMarked;
